        nga.field('{{ field.name }}'{% if field.type != 'string' %}, '{{ field.type }}'{% endif %})
{% if not fromReference|default(false) and field.name == 'id' -%}
    {% if view == 'edition' %}                        .editable(false)
{% endif %}
    {% if view == 'edition' or view == 'show' %}
                    .isDetailLink(false){% endif %}
{%- endif %}
