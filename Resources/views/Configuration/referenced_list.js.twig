                nga.field('{{ field.name }}', '{{ field.type }}')
                    .targetEntity(nga.entity('{{ field.referencedEntity }}'))
                    .targetReferenceField('{{ field.referencedField }}')
                    .targetFields([
{% for field in entities[field.referencedEntity] %}{% if field.type != 'reference' and field.type != 'referenced_list' %}
                        nga.field('{{ field.name }}', '{{ field.type }}')
{%- endif %}{{ loop.last ? "" : "," }}
{% endfor %}                    ])
