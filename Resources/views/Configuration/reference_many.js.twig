nga.field('{{ field.name }}', 'reference_many')
                    .targetEntity(nga.entity('{{ field.referencedEntity.name }}'))
                    .targetField(nga.field('{{ field.referencedField }}'))
